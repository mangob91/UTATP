import React from 'react';
import {welcomeMessage} from '../lib/constant';

const utatpHeader = (props) => {

  return (
    <div>
      {welcomeMessage}
    </div>
  )
}

export default utatpHeader;
